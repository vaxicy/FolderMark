# 2026-08-17 Daily Log

## FolderMark 设置页下拉统一为自定义组件
- 把 `popup.html` 的 `#languageSelect`（6 语言）与 `#actionPositionSelect`（left/right）从原生 `<select>` 改为与 `#themeSelect` 同构的 `.custom-select`。
- `popup.js` 新增 `_initLanguageCustomSelect()` / `_initActionPositionCustomSelect()`（照搬 `_initThemeCustomSelect` 的 trigger toggle + 选项点击 + 文档点击关闭逻辑）。
- `renderSettings()`、`changeLanguage()`、跨扩展同步块都已改用自定义下拉同步（无原生 `.value` 赋值）。
- 6 个 locale 各新增 `langEnglish/langChinese/langSpanish/langJapanese/langKorean/langRussian` 六个 key（对象值），与 en 对齐校验 0 缺失/0 多余/0 类型错误；`actionPositionLeft/Right` 早已存在。
- `node --check popup.js` 通过；`.setting-item .custom-select` 样式已存在，无需改 CSS。
- commit `5e9b43c`（本地提交，Chrome 扩展项目未 push）。
- 涉及 JS/CSS/HTML 改动，用户 Reload 扩展后生效。

## 新增 6 语言商店长描述（storeListingDescription）
- 用户要求「新的商店说明，要有 locale 文件这些语言的版本」。在 6 个 `_locales/*/messages.json` 各新增 `storeListingDescription` 键（值为 `{ "message": "..." }` 对象），内容为约 330–1150 字符的详细商店说明（颜色标签/图标、空&重复文件夹检测、失效书签、健康评分、批量操作+撤销、智能建议、本地处理/隐私、6 语言、浅深主题）。
- 同步刷新短描述 `extensionDescription`（manifest 132 字符上限，现最长 113），强调颜色标签+空/重复检测+失效书签。
- 校验：6 locale 均 290 key 完全对齐、值类型全为 object、short desc ≤132；`store-listing-description-i18n` 品牌名统一用英文 FolderMark。
- 另写 `store-assets/store-description.txt`（=== 语言 === 分段，含 6 语言）便于打包/复用。
- commit `8a6a67f`。商品详情页长描述可从各 locale 的 `storeListingDescription` 取。注意：Chrome Web Store 详情页长描述本身不走 `__MSG_` 占位符，需在各语言商店后台分别粘贴对应语言文案。
