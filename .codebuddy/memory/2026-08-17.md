# 2026-08-17

## 新增俄语（ru）作为第 6 个 UI 语言
- 用户发现 Chrome Web Store 有俄语用户，要求加俄语界面。已一次性全做（含 146 条颜色库俄语翻译）。
- 新增 `_locales/ru/messages.json`（280 个 key，与 en 完全对齐，UTF-8 正常）。
- `popup.js` 改动：
  - `presetList` / `presetMap` 各条目加 `ru:` 字段
  - 辅助方法 `_langName` keyMap 加 `ru:'ru'`；`_colorNameOf` idx 加 `ru:9`；`_customColorLabel` 加 `ru:'Свой цвет'`
  - colorDB 约 146 条追加第 10 列（index 9）俄语名（由英文词级翻译生成）
  - `smartClassify` 关键词映射加俄语（6 色 × 若干词）
  - `formatTime` locale 映射加 `ru:'ru-RU'`
- `popup.html` 的 `#languageSelect` 加 `<option value="ru">Русский</option>`
- 验证：6 个 locale 均 280 key 对齐；`node --check popup.js` 通过；colorDB 每条 7 个引号串（hex+6 名）正确。
- 注：临时脚本 gen_ru.py / add_ru_colors.py 等留在 `C:\Users\16704\`（用户拒绝清理，未删除）。

## 修复俄语 locale 格式导致的大量 undefined/空白标签
- 问题：切换俄语后搜索框 placeholder 显示 `undefined`，多处标签空白。根因是 `_locales/ru/messages.json` 值被写成普通字符串 `"key": "value"`，而 `src/utils/i18n.js` 的 `getMessage` 按 Chrome i18n 标准格式读取 `message.message`，字符串的 `.message` 为 `undefined`。
- 修复：把俄语 locale 全部 280 个 key 的值包装成 `{ "message": "..." }` 对象格式。
- 验证：6 locale 均 object 格式 + 280 key 对齐；`node --check popup.js` 通过。
- 经验：新增 locale 文件后必须检查值类型是否为 `{message:string}`，不能是扁平字符串。
- Chrome 扩展项目不主动 push，等用户指令。

## 新增薰紫主题 lavender
- 用户从图片提供色系（奶油底 #FFF2E0 + 薰衣草紫 #A2AADB / #898AC4 / #C0C9EE），命名为 `lavender / 薰紫`。
- 改动：
  - `popup/popup.css` 新增 `[data-theme="lavender"]` 变量块 + header 渐变 + `.btn-primary` + `.logo/.stat-number` 覆盖。
  - `popup/popup.html` 主题下拉加 `<div class="custom-select-option" data-value="lavender"><span data-i18n="themeLavender">Lavender</span></div>`。
  - `_locales/{en,zh_CN,es,ja,ko,ru}/messages.json` 加 `themeLavender` 键（zh=薰紫，其他为 Lavender/Lavanda/ラベンダー/라벤더/Лаванда）。
- 校验：6 locale 均 key 对齐 + 值类型 object；`node --check popup.js` 通过。
- 已本地提交 ee1a45c，未 push（等用户指令）。

## 新增春雾主题 springmist
- 用户从图片提供低饱和春雾色系（灰紫 #ADB2D4 / 雾蓝 #C7D9DD / 淡绿 #D5E5D5 / 奶黄 #EEF1DA），命名为 `springmist / 春雾`，强调薄雾多色晕染、不与现有主题撞色撞名。
- 改动：
  - `popup/popup.css` 新增 `[data-theme="springmist"]` 变量块 + header 渐变(雾蓝→奶黄) + `.btn-primary`(浅灰紫底#ADB2D4+深字) + `.logo/.stat-number`(深雾紫#5A6080) 覆盖。
  - `popup/popup.html` 主题下拉加 `<div class="custom-select-option" data-value="springmist"><span data-i18n="themeSpringmist">Spring Mist</span></div>`。
  - `_locales/{en,zh_CN,es,ja,ko,ru}/messages.json` 加 `themeSpringmist` 键（zh=春雾，en=Spring Mist，es=Bruma de Primavera，ja=春霧，ko=봄안개，ru=Весенняя дымка）。
- 校验：6 locale 均 key 对齐 + 值类型 object；CSS lint 0 错误；主题总数变 14（light/dark + 12 彩色）。
- 未 push（Chrome 扩展项目，等用户指令）。
