# 2026-07-28

## 失效书签检测改为后台运行（关闭弹窗不中断）
- 根因：原 `scanBrokenBookmarks()` 在 popup.js 内用 `fetch()` 逐个检测，关闭弹窗即中断。
- 方案：检测逻辑迁移到 `background.js`（Service Worker，已改为 ES module + `type: "module"`），
  通过 `chrome.runtime.sendMessage` 三消息协作：`start-broken-scan` / `get-broken-scan-state` / `cancel-broken-scan`。
- background 把进度/结果写入 `chrome.storage.local`（key `brokenScanState`：`{status,completed,total,broken,validCount,error,...}`），进度写入节流 >300ms。
- popup 改为发消息 + 每 400ms 轮询 storage 更新进度条；`init()` 末尾 `restoreBrokenScanState()`：进行中则继续轮询，已完成则直接渲染上次结果。
- 权限请求（用户手势）仍留在 popup，授权后才通知 background 开始。

## 空状态图标发光跟随主题
- `popup.css` 给每个主题块（含 `:root` 默认）加 `--icon-glow` 变量（drop-shadow 颜色，按主题色系）。
- `.empty-state-icon` 的 `filter: drop-shadow(...)` 由硬编码橙色 `rgba(217,119,6,0.2)` 改为 `var(--icon-glow)`。
- 影响 🔗（失效书签空态）与 🎉（无重复/空文件夹庆祝）等空状态图标。

## 健康分数圆环 & 扫描状态 UI 修复
- `.circle`（健康分数 SVG 圆环）`stroke` 从 `var(--success)`（绿）改为 `var(--primary)`，跟随主题色（珊瑚主题→珊瑚红）。
- 失效书签扫描 `sendMessage` 加重试：start 最多 3 次（300ms 退避），轮询/恢复各 1 次，应对 MV3 Service Worker 休眠未唤醒导致的 "Receiving end does not exist" 错误。
- 新增 `_isScanningBroken` 标志位：扫描进行中 `renderBrokenBookmarks()` 显示「⏳ 正在扫描中...」（隐藏开始按钮），不再误显示「尚未扫描」。扫描完成/失败/重试耗尽时复位。
- 新增 i18n：`scanningBroken` / `scanningBrokenDesc`（en + zh_CN）。

## 自定义滚动条（隐藏原生 + 跟随主题）
- 全局规则 `scrollbar-no-native`：隐藏原生滚动条（含箭头），用 JS 驱动主题色滑块。
- `.smart-list`（失效书签/空文件夹/清理建议 3 个列表）加 `position:relative` + `scrollbar-width:none` + `::-webkit-scrollbar{display:none}` + `.smart-list::-webkit-scrollbar-button{display:none}`。
- 顶部全局加 `*::-webkit-scrollbar-button{display:none}`。
- 新增 `.custom-scrollbar`（透明轨道，hover/scrolling 时 opacity:1）/ `.custom-scrollbar-thumb`（默认 `var(--border-color)`，hover `var(--primary)`，跟随主题）。
- popup.js 末尾 IIFE `initCustomScrollbars()`：注入滚动条 DOM → `update()` 算滑块高度（夹 45%~60%）→ 监听 `scroll` 更新 → 拖拽 + 点击轨道跳转 → `ResizeObserver` 刷新。列表内容变化后 `renderBrokenBookmarks()` 调 `window.__refreshScrollbars()`。
- **关键坑已处理**：① 绝对定位子元素会随滚动内容一起移动 → `update()` 用 `top = 4 - scrollTop` 反向偏移把轨道钉在可视区；② module 脚本在 `DOMContentLoaded` 之后才执行 → 用 `readyState` 判断兼容。

## 清空失效书签检测结果按钮
- popup.html：`brokenBookmarksActions` 区新增 `<button id="clearBrokenBtn">`（`btn-secondary btn-sm`），与「全部删除」并排（flex-end + gap）。
- popup.js：新增 `clearBrokenResults()` 方法 → 确认提示（不删除书签）→ 清空 `this.brokenBookmarks` → 重渲染 → 刷新滚动条。`bindEvents` 中绑定按钮 click 事件。
- i18n：新增 `clearBroken`/`confirmClearResults`/`resultsCleared`（en + zh_CN）。

## Bug 修复：清空结果后重开仍显示旧数据
- 根因：`clearBrokenResults()` 只清内存 `this.brokenBookmarks = []`，未清 `chrome.storage.local` 里的 `brokenScanState`，导致重开弹窗 `restoreBrokenScanState()` 从 storage 读回旧结果。
- 修复：clear 回调里 `chrome.runtime.sendMessage({action:'clear-broken-scan-state'})`（带 lastError 容错）；background.js `onMessage` 新增 `clear-broken-scan-state` case → `chrome.storage.local.remove(BROKEN_SCAN_KEY)`。
- 顺手通知组件小优化：`helpers.js` 关闭按钮 `innerHTML='×'` → `textContent='×'`；`.notification-message` 加 `max-height:120px;overflow-y:auto;word-break:break-word` 防长文案溢出。

## UI 优化：搜索栏三元素等高
- `.search-container` 一行内：搜索框 `.search-input`、文件夹按钮 `.search-mode-btn`(📁，本就 28px)、颜色筛选 `.custom-select-trigger`。
- 之前 `.search-input` 与 `.custom-select-trigger` 只靠 padding 撑高度，被 font-size 不同（11px vs 10px）撑出不等高。
- 修复：两者都加 `height: 28px; box-sizing: border-box;`，与 `.search-mode-btn` 统一。父容器已有 `display:flex; align-items:center`。
- 该教训已提炼为全局规则：① `state-storage-sync-RULE.md`（清 UI 状态须同步清 storage）；② 补入 `layout-margin-align.md`（同行控件应等高）。

## Bug 修复：原生 select 下拉箭头贴边（之前修错了）
- 之前只给 `.sort-select` 加 `padding: 4px 24px 4px 8px` 想让箭头不贴边 → 无效：原生 `<select>` 箭头是浏览器引擎渲染、固定在最右边，padding 只移文字不移动箭头。
- 正确修复：`.sort-select` 与 `.setting-item select`（theme/language/actionPosition 三个设置下拉）都加 `-webkit-appearance:none; appearance:none;` 隐藏原生箭头，再用内联 SVG 小箭头（中性灰 #888，深浅主题可见）+ `background: ... no-repeat right 8px/6px center` 精确控制位置。
- 全局规则 `layout-margin-align.md` 的「原生 select 箭头」一节已同步修正为正确做法（原本写错的 padding 方案已替换）。
- 后续优化：箭头从 10×6(stroke 1.5) 缩小到 8×5(stroke 1.2)，与 10px 字体视觉协调；全局规则同步补入「箭头尺寸应与字体比例协调」规则。

## 排查：git-commit-watcher + 通知系统（重要）
- 用户问截图里 `git-commit-watcher started` 窗口是啥。脚本在 `C:\Users\16704\.codebuddy\git-commit-watcher.ps1`（2026-07-23 建），监控 `D:\迅雷下载\vibe coding` 下所有 git 仓库的新 commit，往 `task_signal.txt` 写信号。
- 诊断：该 watcher 当前没在跑；`task_signal.txt` 自 2026-07-23 后再没更新；且 `.codebuddy` 下其实有一整套**可用**通知系统：`task_watcher.ps1`（Windows 气泡）、`codebuddy_notify_watcher.ps1`（BurntToast）都在轮询 `task_signal.txt`。git-commit-watcher 只是其中一个「生产者」且从未运行，所以 Git 提交通知从没弹过，但 AI 每轮结束的弹窗通知是工作的。
- 处理：用户选 A 删除 git-commit-watcher.ps1（已删）。**误删了 task_signal.txt**，意识到它是通知系统核心信号文件后**立即重建恢复**。其余 watcher 文件未动。
- 教训：删 watcher 脚本前必须先确认它读写的是不是被其他（工作）组件共享的信号文件，不能连带删共享文件。
- 后续用户确认整套通知系统没用，全删：`git-commit-watcher.ps1`(早删) + `task_watcher.ps1` + `codebuddy_notify_watcher.ps1` + `notify.ps1`(BurntToast 助手) + `notify_watcher.log` + `setup-watcher-admin.bat` + `start-watchers.bat` + `task_signal.txt`。排查确认无计划任务、无启动项(PinMateNotify.bat 不存在，启动文件夹只有 popup-codebuddy.bat)、无运行进程——系统本就休眠。rules 目录无 task_signal 引用，无全局规则需清理。

## 新增 2 个主题（方案 A，凑到 10 个）
- 用户选方案 A：补 玫红 `rose` + 薄荷 `mint`，主题总数到 10。
- 4 处改动：`popup.css` 各加 5 段（变量块 + header 渐变 + btn-primary 浅底深字 + hover + logo/stat-number 深同色系）；`popup.html` 下拉加 2 个 `<option>`；`_locales/en` 与 `zh_CN` 各加 `themeRose`/`themeMint` 键。中文名「玫红/薄荷」。
- 颜色：rose 主色 #D14B66、header 渐变 #F2A0B4→#FCEEF1、logo/数字 #C14B66；mint 主色 #2EAE9A、header 渐变 #7FD8C4→#E9F6F1、logo/数字 #1E9C8A。均遵循 WCAG AA（浅底深字）。
- 已校验：两 locale JSON 合法、CSS 无 lint。MEMORY.md 主题清单已更新为 10 个。
- 微调：用户反馈玫红与腮粉太像。把玫红往更红更深的绯红拉：primary #D14B66→#C73E5C、header 顶端 #F2A0B4→#E86A87、btn 底 #F2A0B4→#F0A0AE、logo/数字 #C14B66→#B22E47。腮粉与薄荷不变。CSS 无 lint。
- header 内文字再加深（玫红主题）：`.logo` #B22E47→#8B1E35（深绯红）、新增 `[data-theme="rose"] .tagline` 加深到 #8D4559，解决粉底上标题/副标题对比不足。CSS 无 lint。

