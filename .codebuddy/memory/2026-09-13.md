# 2026-09-13

## 新增法语（fr）国际化支持（第 7 种语言）
- 按既有"新增语言标准流程"落地，全程未改 manifest / 权限。
- 新建 `_locales/fr/messages.json`：291 个 key 与 en 完全对齐，值均为 `{message:...}` 对象（已校验无缺失/多余 key、无扁平字符串）。品牌名 FolderMark 不译；storeListingDescription 三语段落已加 Français。
- `popup.js` 接入 fr：
  - `_langName` keyMap 加 `fr:'fr'`；`_colorNameOf` idx 加 `fr:10`；`_customColorLabel` 加 `fr:'Couleur personnalisée'`；`formatTime` locale 加 `fr:'fr-FR'`。
  - `presetList`(6) / `presetMap`(8) / `colorNames`(7) / `sizeRanges`(5) 各条目加 `fr` 字段。
  - `smartClassify` 关键词映射 6 色各补法语词。
  - `colorDB`(~146 条) 每条追加第 10 列（法语色名）；用脚本批量写入，修复了一次多产生 `]` 的 bug（scoped 替换 `'], '`→`', '`）。
- `popup.html` 语言下拉在 ru 后加 `<div data-value="fr" data-i18n="langFrench">Français</div>`。
- 6 个现有 locale 各补 `langFrench` 键（JSON 脚本批量加，291 key 对齐）。
- 校验：`node --check popup.js` 通过；fr 与 en 键集合一致、值类型全为 object；6 locale 互相对齐。
- 提交为 `feat: 新增法语(fr)国际化支持`。
- 后续用户反馈：语言下拉在中文界面里 English、Français 未中文化。已将 `_locales/zh_CN/messages.json` 的 `langEnglish` 改为「英语」、`langFrench` 改为「法语」，与其他语言选项保持一致。提交 `4a92c41` 已推送。
- 继续反馈：韩语/俄语/法语界面下语言选项未统一成本地语言。已统一调整 ko/ru/fr/en/es/ja 的 `langFrench`/`langEnglish`/`langSpanish`：法语在 ko 显示「프랑스어」、ru「Французский」、en「French」、es「Francés」、ja「フランス語」；法语界面中英语显示「Anglais」、西班牙语显示「Espagnol」。提交 `6aeac5b` 已推送。
- 新增圣诞主题「冬青 Holly」（key `holly`）：配色暗酒红 #6D0808 + 奶油 #EEEAD7 + 灰绿 #757D6F，主按钮用浅灰绿呼应红配绿。改动 popup.css（变量块+header 渐变+按钮/文字覆盖）、popup.html（下拉选项）、7 个 locale 新增 themeHolly 键。提交 `f582eb0` 已推送。主题总数 16 个。
- 根据用户反馈，冬青主题 header 渐变从暗酒红→奶油差值过大，改为同色系微渐变 #6D0808 → #7A0F0F，接近纯色暗酒红。提交 `f89c535` 已推送。
- 基于用户提供配色新增主题：橄榄绿 #84994F + 奶油黄 #FFE797 + 金橙 #FCB53B + 陶土红 #B45253。先命名为 Cider（苹果酒），后按用户反馈本地化不自然且不像苹果，重命名为 Autumn（秋日），并完成 7 语言本地化。提交 `db4a237`、`9b70fda` 已推送。
- 基于用户提供配色新增「Aqua」主题：亮水蓝 #30AFFF + 浅青 #92EEFF + 薄荷绿 #D8FFC5/#C4F7CA；与 Sky/Teal/Mint 不撞色，7 语言本地化：en Aqua / zh_CN 碧波 / es Aguamarina / ja アクア / ko 아쿠아 / ru Аква / fr Aqua。提交 `ee9a90f` 已推送；中文名历经「水色」→「空青」(`d66e55a`)→「碧波」(`c585058`)。
- 基于用户提供紫→玫红调色板新增「Nebula（星云）」主题：深紫 #462C7D / 电光紫 #831C91 / 玫粉 #D552A3 / 亮粉 #FF70BF；主色 #831C91，浅紫粉背景，header 浅玫粉→浅紫微渐变；与 鸢尾/薰紫/藕荷/玫红/腮粉/珊瑚 明显区分不撞色。7 语言本地化：en Nebula / zh_CN 星云 / es Nebulosa / ja ネビュラ / ko 네뷸라 / ru Туманность / fr Nébuleuse。提交 `1dcdf42` 已推送。
- 基于用户提供暖黄→橙→珊瑚粉调色板新增「Sunrise（晨曦）」主题：奶油黄 #FFEDB9 / 亮金黄 #FFCB56 / 暖橙 #FFA259 / 珊瑚粉 #FF7E7E。最初主色 #FFA259 + 奶油黄背景，用户反馈与浅色主题撞车；调整为蜜桃粉基调：bg #FFF5F0/#FFE8E0/#FFD4C4，主色 #E86A3F，header 渐变 #FFCB56 → #FF7E7E，按钮亮金黄 #FFCB56 + 深棕字。与浅色/秋日/珊瑚 明显区分。7 语言本地化：en Sunrise / zh_CN 晨曦 / es Amanecer / ja サンライズ / ko 선라이즈 / ru Рассвет / fr Aube。提交 `5cd1b75`、`9d8d46b` 已推送。主题总数 20 个。
