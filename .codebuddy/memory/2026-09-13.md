# 2026-09-13

## 实现 8 项视觉/功能建议（用户点选：视觉 1/4/5、功能 1/2/3/4、体验 3）
- 视觉1+4：主题下拉动态构建（`THEME_LIST` + `buildThemeOptions`），含主色预览圆点 + 按色相分组（基础/暖色/冷色/中性）；`_initThemeCustomSelect` 改为事件委托。
- 视觉5：`.folder-card` hover 微交互（CSS）。
- 功能1：配色方案导出/导入（按文件夹路径，跨设备可移植），设置页新增「配色方案」分区。
- 功能2：按域名自动配色（🎯 按钮 + `DOMAIN_COLOR_MAP`/`hashToPreset`）。
- 功能3：多选批量改色——确认已有（`selectedFolderIds`+batch modal），未重复实现。
- 功能4：一键彩虹渐变（🌈 按钮，HSL 均匀铺开）。
- 体验3：空状态改为 SVG 插图（`emptyIllustrationSVG`）。
- 批量改色统一走 `runBulkColor` + 通知「撤销」→`restoreColorMap`。
- 7 个 locale 文件均已加新 key（en/zh_CN 翻译，其余英文兜底）；JSON 校验通过。
- 校验：`node --check popup/popup.js`、`node --check src/utils/constants.js` 均 OK；7 locale JSON parse OK。

## 通知提示布局优化（同日追加）
- 将 undo-action 与 close × 按钮包进 `.notification-actions`，通知改为横向 flex：左侧图标+消息，右侧「撤销」+「×」。
- 增加通知最大宽度（220px→260px）、内边距，避免操作按钮被挤到消息下方。涉及 `src/utils/helpers.js` 与 `popup/popup.css`。

## 全动作支持撤销（同日追加）
- 新增统一方法 `_commitColorChanges(changes, msgKey, fallback, previousMap)`：记录改色前颜色 → 套用 → 弹带「撤销」通知（→`restoreColorMap`）。
- 为以下原本无撤销的改色动作接入：`_commitColorChanges`：单文件夹预设色、单文件夹自定义色（打开时记 `_customColorOriginal`）、批量预设色、批量自定义色（记 `_batchColorOriginal`）、智能分类应用。均已带撤销。
- `renameFolder` 新增撤销（重命名后弹通知，撤销改回原名）。
- 已自带撤销（无需改）：删除/合并（走 `UndoService`+`showUndoToast`）、批量自动配色/彩虹/导入配色方案（走 `restoreColorMap`+通知 action）、整库导入 `importStructure`。
- 新增 i18n key `colorApplied`（en/zh_CN 译，其余英文兜底）。

## 合并撤销修复（同日追加）
- 此前 `doMerge` 调用 `FolderOperations.deleteFolder` 直接删除源文件夹，**无撤销**（我的「合并走删除撤销」判断有误）。
- 重写为：合并前用 `BookmarkService.getNode` 记源文件夹 {title,parentId,index}，用 `getChildren` 记录全部子节点 id（书签+子文件夹，移动后 id 不变）；合并后弹带「撤销」通知，撤销时 `createFolder` 重建源文件夹（并尝试还原 index），再把所有子节点 id 移回，从而完整还原。
- 新增 i18n `mergeUndoDone`（en/zh_CN 译，其余自动回退 en）。

## 合并接入撤销历史面板（同日追加）
- 用户确认要把合并也进撤销历史面板。
- `UndoService` 新增：`pushMerge(data, desc)`（type:'merge'，返回条目引用）、`removeEntry(entry)`、`undoMerge(data)`（重建源文件夹+移回全部子节点）；`undo()` 增加 `entry.type==='merge'` 分支委托 `undoMerge`。
- `popup.doMerge`：合并后 `UndoService.pushMerge(mergeUndo, title)` 入栈；成功通知的「撤销」按钮回调里先 `removeEntry` 再 `undoMerge`（避免面板重复撤销），并 `renderUndoHistory()`。面板单条撤销走原有 `executeUndo → undo()` 分支，自动兼容合并类型。
- 校验：`node --check` 两文件通过，lint 无错误。

## 打包上传商店（同日追加）
- 用户要升版本号并打包到默认文件夹 `dist/`（含 `_pkg` 暂存 + 按版本命名的 `FolderMark-<ver>.zip`）上传商店。
- 版本 1.1.2 → **1.2.0**（minor，新增功能：合并撤销进历史面板、配色撤销等）。
- 打包方式：`dist/_pkg` 为解压态扩展（manifest.json, background.js, popup/, src/, icons/, _locales/），`FolderMark-<ver>.zip` 即 `_pkg` 内容压缩。用 PowerShell 重建 `_pkg` 并从根目录同步运行时文件后 `Compress-Archive`。
- 坑：命令行直接传中文路径会被 shell 编码破坏；改用 `Get-ChildItem -Path 'D:\*\vibe coding\Chrome Extensions\FolderMark'` 通配符解析中文目录，且命令行不含中文/$ 变量，避免被 shell 展开。
- 产出：`dist/FolderMark-1.2.0.zip`（约 321KB），manifest 内 version=1.2.0，含最新 undoMerge/pushMerge 代码。已清理临时脚本。

## 打包位置修正（同日追加）
- 用户指出默认文件夹是项目根目录（`Chrome Extensions\FolderMark`），不是 `dist/`；已写入 `MEMORY.md` 全局规则：商店 zip 默认输出到项目根目录，`dist/` 仅保留 `_pkg` 暂存。
- 已重新打包：`FolderMark-1.2.0.zip` 生成在项目根目录（约 321KB），旧的 `dist/FolderMark-1.2.0.zip` 已删除。
- 已 `git add -u` + `git commit -m 'Release v1.2.0'` + `git push origin main` 成功（16 files changed, 1184 insertions, 104 deletions）。根目录 zip 未加入 git（untracked），留给用户自行上传商店；如需进仓库可单独 `git add FolderMark-1.2.0.zip`。

## 注意
- `dist/` 是打包副本，与 `popup/` 不同步；改源码后发布前需手动重新打包。
- 主题下拉 `popup.html` 里静态 `<option>` 保留作降级兜底。
